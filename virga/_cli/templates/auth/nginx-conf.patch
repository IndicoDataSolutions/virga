--- nginx/app-indico-local.conf
+++ nginx/app-indico-local.conf
@@ -28,4 +28,4 @@
-	#location /auth {
-	#	rewrite ^/auth/(.*) /$1 break;
-	#	proxy_pass http://noct:5000/;
-	#}
+	location /auth {
+		rewrite ^/auth/(.*) /$1 break;
+		proxy_pass http://noct:5000/;
+	}
